{{define "content"}}
<div class="auth-page register-page">
    <div class="auth-container">
        <div class="auth-header">
            <h1>{{t "auth.register"}}</h1>
            <p class="auth-subtitle">{{t "auth.register.title"}}</p>
        </div>

        {{if .Error}}
        <div class="alert alert-error" role="alert">
            {{.Error}}
        </div>
        {{end}}

        {{if .Success}}
        <div class="alert alert-success" role="alert">
            {{t "auth.register.success"}}
            {{if .RequireEmailVerification}}
            <p>{{t "auth.register.verify_email"}}</p>
            {{end}}
        </div>
        {{else}}
        <form method="POST" action="/auth/register" class="auth-form" id="register-form">
            <input type="hidden" name="csrf_token" value="{{.CSRFToken}}">

            <div class="form-group">
                <label for="username">{{t "auth.register.username"}} <span class="required">*</span></label>
                <input type="text" id="username" name="username" value="{{.FormData.Username}}"
                       autocomplete="username" required autofocus
                       pattern="[a-z0-9_-]+" minlength="3" maxlength="32"
                       aria-describedby="username-help">
                <small id="username-help" class="form-help">
                    {{t "validation.username.invalid"}}
                </small>
            </div>

            <div class="form-group">
                <label for="email">{{t "auth.register.email"}} <span class="required">*</span></label>
                <input type="email" id="email" name="email" value="{{.FormData.Email}}"
                       autocomplete="email" required
                       aria-describedby="email-help">
                <small id="email-help" class="form-help">
                    {{t "validation.email"}}
                </small>
            </div>

            <div class="form-group">
                <label for="password">{{t "auth.register.password"}} <span class="required">*</span></label>
                <div class="password-input-wrapper">
                    <input type="password" id="password" name="password"
                           autocomplete="new-password" required
                           minlength="8"
                           aria-describedby="password-help">
                    <button type="button" class="password-toggle" aria-label="Toggle password visibility">
                        <span class="icon-eye"></span>
                    </button>
                </div>
                <small id="password-help" class="form-help">
                    {{t "validation.min_length" 8}}
                </small>
                <div class="password-strength" id="password-strength"></div>
            </div>

            <div class="form-group">
                <label for="confirm_password">{{t "auth.register.confirm_password"}} <span class="required">*</span></label>
                <div class="password-input-wrapper">
                    <input type="password" id="confirm_password" name="confirm_password"
                           autocomplete="new-password" required
                           aria-describedby="confirm-help">
                    <button type="button" class="password-toggle" aria-label="Toggle password visibility">
                        <span class="icon-eye"></span>
                    </button>
                </div>
                <small id="confirm-help" class="form-help error-text hidden">
                    {{t "validation.password_match"}}
                </small>
            </div>

            {{if .InviteCode}}
            <input type="hidden" name="invite_code" value="{{.InviteCode}}">
            {{end}}

            <button type="submit" class="btn btn-primary btn-block" id="submit-btn">
                {{t "auth.register.submit"}}
            </button>
        </form>
        {{end}}

        <div class="auth-footer">
            <p>{{t "auth.register.have_account"}} <a href="/auth/login">{{t "auth.login"}}</a></p>
        </div>
    </div>
</div>
{{/* Per AI.md PART 16: NO inline CSS/JS - all styles in main.css, JS in app.js */}}
{{end}}
