{{define "public/footer"}}
<footer class="footer">
    {{/* Tor hidden service - per AI.md PART 32 (always show if enabled) */}}
    {{if .TorEnabled}}
    <div class="tor-access">
        <span class="tor-icon">🧅</span>
        {{if .TorAddress}}
        <div class="code-block">
            <code class="code-content onion-address">{{.TorAddress}}</code>
            <button type="button" class="copy-btn" data-copy="{{.TorAddress}}" aria-label="Copy to clipboard">
                <span class="copy-icon">📋</span>
            </button>
        </div>
        {{else}}
        <span class="tor-status">Tor: {{.TorStatus}}</span>
        {{end}}
    </div>
    {{end}}

    {{/* Standard page links (always first) - per AI.md PART 17 */}}
    <p>
        <a href="/server/about">About</a>
        <span>•</span>
        <a href="/server/privacy">Privacy</a>
        <span>•</span>
        <a href="/server/contact">Contact</a>
        <span>•</span>
        <a href="/server/help">Help</a>
    </p>

    <br />

    {{/* Application branding - per AI.md PART 17 */}}
    <p>
        {{if .Config.Server.Branding.SourceCodeURL}}<a href="{{.Config.Server.Branding.SourceCodeURL}}" target="_blank" rel="noopener">Made with</a>{{else}}Made with{{end}} ❤️
        <span>•</span>
        <span>{{version}}</span>
    </p>

    <br />

    <a href="/healthz">Last update: {{.BuildDate}}</a>
</footer>
{{end}}
