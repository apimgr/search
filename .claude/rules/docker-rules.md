# Docker Rules (PART 26)

Read: `AI.md` PART 26 (Docker).

- `docker/Dockerfile` is the only Dockerfile location — never a root-level Dockerfile.
- `docker/Dockerfile.dev`, `docker/docker-compose.yml`, `docker/docker-compose.dev.yml`, `docker/docker-compose.test.yml` support local dev/test workflows.
- Follow OCI label and binary naming conventions from PART 26 exactly.
