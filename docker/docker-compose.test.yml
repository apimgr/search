name: search-test

x-logging: &default-logging
  options:
    max-size: "5m"
    max-file: "1"
  driver: json-file

services:
  search:
    image: ghcr.io/apimgr/search:latest
    pull_policy: always
    container_name: search-test
    hostname: search
    restart: "no"
    logging: *default-logging
    environment:
      PORT: 80
      DEBUG: 1
      MODE: dev
      TZ: America/New_York
      CACHE_URL: valkey://search-cache-test:6379
    volumes:
      # CRITICAL: ./volumes/ must resolve to $TEMP_DIR/volumes/, NOT project directory
      # This file MUST be copied to a temp directory before running
      # AI: NEVER run this from the project directory
      - ./volumes/config:/config:z
      - ./volumes/data:/data:z
    ports:
      - "172.17.0.1:64581:80"
    healthcheck:
      test: ["CMD", "/usr/local/bin/search", "--status"]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 90s
    depends_on:
      search-cache-test:
        condition: service_healthy
    networks:
      - search-test

  search-cache-test:
    image: valkey/valkey:alpine
    pull_policy: always
    container_name: search-cache-test
    restart: "no"
    logging: *default-logging
    tmpfs:
      - /data
    healthcheck:
      test: ["CMD-SHELL", "valkey-cli ping || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 30s
    networks:
      - search-test

networks:
  search-test:
    name: search-test
    external: false
